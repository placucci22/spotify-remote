name: Scrape Liga Pokémon

on:
  schedule:
    - cron: "0 9 * * *"   # 06:00 BRT (UTC-3)
  workflow_dispatch:        # manual trigger

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python packages
        run: pip install requests beautifulsoup4 "playwright==1.44.0"

      - name: Install Playwright browser
        run: python -m playwright install chromium

      - name: Install Playwright system dependencies
        run: sudo python -m playwright install-deps chromium

      - name: Scrape Liga Pokémon and store in Redis
        env:
          KV_REST_API_URL: ${{ secrets.KV_REST_API_URL }}
          KV_REST_API_TOKEN: ${{ secrets.KV_REST_API_TOKEN }}
        run: python scripts/scrape_liga.py
