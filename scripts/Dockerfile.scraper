FROM python:3.11-slim
WORKDIR /app
COPY scripts/scrape_liga.py scrape_liga.py
RUN pip install requests beautifulsoup4 playwright "playwright-stealth==1.0.6" && \
    playwright install chromium --with-deps
CMD ["python", "scrape_liga.py"]
